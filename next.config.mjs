// next.config.mjs
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['fakestoreapi.com'], // Add the hostname "fakestoreapi.com" to the list of domains
  },
};

export default nextConfig;
